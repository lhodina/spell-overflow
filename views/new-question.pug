extends layout.pug

include utils.pug

block content
    form(action="/questions/new" method="post")
        input(type="hidden" name="_csrf" value=csrfToken)
        +field("Title", "title", "", "textarea")
        +field("Ask a public question", "headline", "", "textarea")
        button Post your question
