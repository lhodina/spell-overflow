extends layout.pug



block content
    form(action=`/questions/delete/${questionId}` method='post')
      input(type='hidden' name='_csrf' value=csrfToken)

      button Delete Question
      a(href=`/question/${questionId}` role='button') Cancel
